# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
.kotlin/

# Local vault files (user financial data must never be committed)
*.pvault
*.pvault-wal
*.pvault-shm

# Real bank statements used for local development — NEVER commit personal data
/sample/
# Local scratch / imported vaults for debugging — may contain personal data
/tmp/

# Bundled embedding model (~128MB) — fetched at build time, not committed
/core/classification/src/main/resources/embedding/


# IDE
.idea/
*.iml
/local.properties

# OS
.DS_Store
